{
  "name": "fastify-compression",
  "version": "0.2.0",
  "description": "A compression plugin for fastify",
  "main": "index.js",
  "scripts": {
    "unit": "tap test/*",
    "test": "npm run lint && npm run unit",
    "lint": "./node_modules/eslint/bin/eslint.js index.js lib/*",
    "coveralls": "npm run unit --  --cov",
    "coverage-report": "npm run coveralls && tap --coverage-report=lcov"
  },
  "keywords": [
    "fastify",
    "compression",
    "gzip",
    "deflate"
  ],
  "author": "Denis Fäcke",
  "license": "MIT",
  "dependencies": {
    "compressible": "^2.0.12",
    "fastify-plugin": "^0.1.1",
    "pump": "^2.0.0",
    "string-to-stream": "^1.1.0"
  },
  "engines": {
    "node": ">= 6.0.0"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/SerayaEryn/fastify-compression.git"
  },
  "devDependencies": {
    "eslint": "^4.10.0",
    "fastify": "^0.35.0",
    "request": "^2.83.0",
    "tap": "^10.7.2"
  }
}
